import std::io::{ println };

void strcopy(^char a, ^char b) => {
	^char og = a;
	while @a != '\0' => @b++ = @a++;

	@b = '\0';
	a = og;
}


int main => {
	let a = "Hello";
	[6]char b;

	strcopy(a, b);

	println("a: {}, b: {}", a, b); // => a: "Hello", b: "Hello"
}
